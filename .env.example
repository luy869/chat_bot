# GPU設定（デフォルト: 単一GPU対応）
# ほとんどの環境で以下のデフォルト設定で動作
# GPU_DEVICES=0
# OLLAMA_PARALLEL=1

# === 本番環境用（複数GPU搭載）===
# GPU 2枚の場合:
# GPU_DEVICES=0,1
# OLLAMA_PARALLEL=2

# GPU 4枚の場合:
# GPU_DEVICES=0,1,2,3
# OLLAMA_PARALLEL=4

# === GPU不要な環境 ===
# GPU を使わない場合は設定を削除（デフォルト値のままで OK）
