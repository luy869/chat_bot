services:
  app:
    build: ./backend
    container_name: rag-platform
    volumes:
      - ./backend:/app
      - /app/.venv
    environment:
      - OLLAMA_HOST=http://172.17.0.1:11434
      - API_KEY=${API_KEY}
    ports:
      - "8000:8000"
    restart: unless-stopped
